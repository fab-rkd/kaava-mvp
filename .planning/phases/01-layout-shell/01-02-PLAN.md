---
phase: 01-layout-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/layout/InnerHeader.tsx
  - components/layout/Footer.tsx
autonomous: true
requirements: [LAY-03, LAY-04]
must_haves:
  truths:
    - "InnerHeader renders a two-row header suitable for category, product, cart, and checkout pages"
    - "Footer renders a 4-column links grid with Kaava branding"
  artifacts:
    - path: "components/layout/InnerHeader.tsx"
      provides: "Two-row header for inner pages (category, product, cart, checkout)"
      min_lines: 50
    - path: "components/layout/Footer.tsx"
      provides: "4-column footer with links grid"
      min_lines: 40
  key_links:
    - from: "components/layout/InnerHeader.tsx"
      to: "context/CartContext.tsx"
      via: "useCart hook for cart badge"
      pattern: "useCart"
    - from: "components/layout/InnerHeader.tsx"
      to: "next/link"
      via: "Link component for navigation"
      pattern: "import Link"
---

<objective>
Build the InnerHeader and Footer layout components used by non-homepage routes.

Purpose: Inner pages need a compact two-row header (logo + nav on row 1, breadcrumb area on row 2) and a 4-column footer. These components are independent of the homepage header.
Output: Two new layout components ready to be wired into page shells.
</objective>

<execution_context>
@/Users/robindhiman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robindhiman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

IMPORTANT: Before writing any Next.js code, read the relevant guide in node_modules/next/dist/docs/01-app/01-getting-started/ to confirm API conventions for this version (Next.js 16.2.0).

<interfaces>
From context/CartContext.tsx:
```typescript
export function useCart(): CartContextType;
// CartContextType includes: state, totalItems, totalPrice, addItem, removeItem,
// updateQuantity, openDrawer, closeDrawer, clearCart
```

From tailwind.config.ts -- design tokens:
```
colors: forest (#2D6A4F), forest-dark (#1B4D3E), forest-light (#40916C), forest-nav (#245c44)
        saffron (#E85D04), saffron-light (#F48C06), gold (#FFBA08)
        kaava.dark (#1B1B1B), kaava.border (#F0F0F0), kaava.surface1 (#FAFAFA)
        kaava.surface2 (#F7F7F7), kaava.muted (#888888), kaava.secondary (#666666)
fontFamily: font-outfit, font-inter, font-devanagari
maxWidth: max-w-content (1200px)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InnerHeader component</name>
  <files>components/layout/InnerHeader.tsx</files>
  <read_first>
    - tailwind.config.ts (design tokens)
    - context/CartContext.tsx (useCart hook for cart badge)
    - types/index.ts (type references)
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
  </read_first>
  <action>
    Create a client component ("use client") with a two-row header for inner pages.

    **Row 1 -- Main header bar:**
    - Outer: w-full bg-white border-b border-kaava-border
    - Inner: max-w-content mx-auto px-4 py-3 flex items-center justify-between
    - LEFT: Logo -- Link to "/" wrapping a span with "Kaava" in font-outfit text-xl font-bold text-forest
    - CENTER: Compact search bar -- input[type="text"] with placeholder "Search products..." styled with border border-kaava-border rounded-full px-4 py-2 text-sm font-inter w-full max-w-sm focus:outline-none focus:border-saffron
    - RIGHT: flex items-center gap-4
      - Link to "/" with text "Home", text-sm font-inter text-kaava-secondary hover:text-forest
      - Cart button with relative positioning, SVG shopping bag icon (20x20, text-kaava-dark), badge span (absolute -top-1 -right-2 bg-saffron text-white text-[10px] rounded-full w-4 h-4 flex items-center justify-center font-semibold). Show badge only when totalItems > 0. Button calls openDrawer.

    **Row 2 -- Navigation bar:**
    - Outer: w-full bg-kaava-surface1
    - Inner: max-w-content mx-auto px-4 py-2 flex items-center gap-6
    - Navigation links (all Link components from next/link): "All Concerns" (href="/"), "Hair" (href="/category/hair"), "Skin" (href="/category/skin"), "Digestion" (href="/category/digestion"), "Immunity" (href="/category/immunity")
    - Link styling: text-sm font-inter text-kaava-secondary hover:text-forest transition-colors

    - Import Link from "next/link"
    - Import { useCart } from "@/context/CartContext"
    - Export as default: `export default function InnerHeader()`
  </action>
  <verify>
    <automated>cd /Users/robindhiman/Desktop/Clients/HerbalMarketPlace/kaava-mvp && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File components/layout/InnerHeader.tsx exists and starts with "use client"
    - Contains 'import Link from "next/link"'
    - Contains 'import { useCart }' or 'import {useCart}'
    - Contains "totalItems" for badge count
    - Contains "openDrawer" for cart button
    - Contains "bg-kaava-surface1" for nav row background
    - Contains "max-w-content" for container constraint
    - Contains "font-outfit" for logo
    - Contains links with href="/category/hair" and href="/category/skin"
    - Contains "export default function InnerHeader"
    - npx tsc --noEmit passes with zero errors related to this file
  </acceptance_criteria>
  <done>InnerHeader renders a two-row header: row 1 has logo, search, cart icon; row 2 has category navigation links. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create Footer component</name>
  <files>components/layout/Footer.tsx</files>
  <read_first>
    - tailwind.config.ts (design tokens)
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
  </read_first>
  <action>
    Create a SERVER component (no "use client" needed -- Footer is purely static content, no hooks).

    Structure:
    - Outer: w-full bg-forest-dark (Tailwind class for #1B4D3E) text-white
    - Inner: max-w-content mx-auto px-4 py-12
    - Grid: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8

    **Column 1 -- Brand:**
    - "Kaava" in font-outfit text-2xl font-bold text-white mb-2
    - Tagline: "India's Verified Ayurveda Marketplace" in text-sm text-white/70 font-inter mb-4
    - Paragraph: "Every product is AYUSH-licensed and lab-tested for purity." in text-sm text-white/60 font-inter

    **Column 2 -- Shop:**
    - Heading: "Shop" in font-outfit text-base font-semibold text-white mb-4
    - Links list (ul with flex flex-col gap-2): "All Products" (/), "Hair Care" (/category/hair), "Skin Care" (/category/skin), "Digestion" (/category/digestion), "Immunity" (/category/immunity)
    - Link styling: text-sm font-inter text-white/70 hover:text-white transition-colors
    - Use Link from next/link

    **Column 3 -- Company:**
    - Heading: "Company" in same style as Column 2
    - Links: "About Us" (/), "Our Story" (/), "AYUSH Compliance" (/), "Contact" (/)
    - Same link styling

    **Column 4 -- Support:**
    - Heading: "Support" in same style
    - Links: "FAQs" (/), "Shipping Policy" (/), "Return Policy" (/), "Track Order" (/)
    - Same link styling

    **Bottom bar:**
    - Separator: border-t border-white/20 mt-8 pt-6
    - Flex justify-between items-center
    - Left: "2026 Kaava. All rights reserved." in text-sm text-white/50 font-inter
    - Right: "Made with care in India" in text-sm text-white/50 font-inter

    - Import Link from "next/link"
    - Export as default: `export default function Footer()`
  </action>
  <verify>
    <automated>cd /Users/robindhiman/Desktop/Clients/HerbalMarketPlace/kaava-mvp && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File components/layout/Footer.tsx exists
    - Does NOT contain "use client" (server component)
    - Contains 'import Link from "next/link"'
    - Contains "bg-forest-dark" for footer background
    - Contains "grid" and "lg:grid-cols-4" for 4-column layout
    - Contains "Kaava" brand text
    - Contains "AYUSH" in compliance text
    - Contains href="/category/hair" link
    - Contains "All rights reserved" in bottom bar
    - Contains "export default function Footer"
    - npx tsc --noEmit passes with zero errors related to this file
  </acceptance_criteria>
  <done>Footer renders a 4-column links grid with brand info, shop links, company links, support links, and a bottom copyright bar. Styled with forest-dark background and white text.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero TypeScript errors
2. InnerHeader.tsx and Footer.tsx exist in components/layout/
3. InnerHeader is a client component with useCart integration
4. Footer is a server component with 4-column grid layout
</verification>

<success_criteria>
- InnerHeader renders two-row header with logo, search, cart badge, and category nav
- Footer renders 4-column links grid with brand, shop, company, support sections
- Both components use Kaava design tokens (forest, saffron, font-outfit, font-inter)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-shell/01-02-SUMMARY.md`
</output>
