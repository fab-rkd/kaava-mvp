---
phase: 01-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/layout/AnnouncementBar.tsx
  - components/layout/HomepageHeader.tsx
  - app/layout.tsx
autonomous: true
requirements: [LAY-01, LAY-02]
must_haves:
  truths:
    - "AnnouncementBar renders a rotating promo message bar at the top of every page"
    - "HomepageHeader renders with logo text, search bar, nav links, and cart icon with badge"
    - "Root layout includes AnnouncementBar so it appears on all pages"
  artifacts:
    - path: "components/layout/AnnouncementBar.tsx"
      provides: "Rotating promo messages bar"
      min_lines: 30
    - path: "components/layout/HomepageHeader.tsx"
      provides: "Full-width homepage header with logo, search, nav, cart"
      min_lines: 50
    - path: "app/layout.tsx"
      provides: "Root layout with AnnouncementBar integrated"
      contains: "AnnouncementBar"
  key_links:
    - from: "app/layout.tsx"
      to: "components/layout/AnnouncementBar.tsx"
      via: "import and render above CartProvider children"
      pattern: "import.*AnnouncementBar"
    - from: "components/layout/HomepageHeader.tsx"
      to: "context/CartContext.tsx"
      via: "useCart hook for cart badge count"
      pattern: "useCart"
---

<objective>
Build the AnnouncementBar and HomepageHeader layout components, then wire AnnouncementBar into the root layout so it appears on every page.

Purpose: Establishes the top-of-page visual shell -- the promo bar and homepage navigation that users see first.
Output: Two new components and an updated root layout.
</objective>

<execution_context>
@/Users/robindhiman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robindhiman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

IMPORTANT: Before writing any Next.js code, read the relevant guide in node_modules/next/dist/docs/01-app/01-getting-started/ to confirm API conventions for this version (Next.js 16.2.0). In particular, params is a Promise that must be awaited, and PageProps/LayoutProps are global helpers (no import needed).

<interfaces>
<!-- Key types and contracts the executor needs -->

From types/index.ts:
```typescript
export type Product = {
  sku: string;
  brand: string;
  name: string;
  // ... full product shape
};
export type CartItem = Product & { quantity: number };
export type CartState = { items: CartItem[]; isDrawerOpen: boolean };
```

From context/CartContext.tsx:
```typescript
export function useCart(): CartContextType;
// CartContextType includes:
//   state: CartState
//   totalItems: number
//   totalPrice: number
//   addItem, removeItem, updateQuantity, openDrawer, closeDrawer, clearCart
```

From tailwind.config.ts -- design tokens:
```
colors: forest (#2D6A4F), forest-dark (#1B4D3E), forest-light (#40916C), forest-nav (#245c44)
        saffron (#E85D04), saffron-light (#F48C06), gold (#FFBA08)
        kaava.dark (#1B1B1B), kaava.border (#F0F0F0), kaava.surface1 (#FAFAFA)
        kaava.surface2 (#F7F7F7), kaava.muted (#888888), kaava.secondary (#666666)
fontFamily: font-outfit, font-inter, font-devanagari
maxWidth: max-w-content (1200px)
```

From app/globals.css:
```css
body { font-family: "Inter", ...; color: #1b1b1b; }
:focus-visible { outline: 2px solid #e85d04; outline-offset: 2px; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnnouncementBar and HomepageHeader components</name>
  <files>components/layout/AnnouncementBar.tsx, components/layout/HomepageHeader.tsx</files>
  <read_first>
    - tailwind.config.ts (design tokens: colors, fonts, maxWidth)
    - context/CartContext.tsx (useCart hook signature for cart badge)
    - types/index.ts (Product, CartItem types)
    - app/globals.css (base styles)
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md (client vs server component rules)
  </read_first>
  <action>
    **AnnouncementBar.tsx** -- Must be a client component ("use client") because it uses useState/useEffect for rotation.

    Create a full-width bar at the top of the page with these specs:
    - Background: bg-forest (Tailwind class for #2D6A4F)
    - Text: text-white, text-sm, font-inter, text-center
    - Padding: py-2 px-4
    - Hardcoded array of 3 promo messages:
      1. "Free shipping on orders above Rs.499"
      2. "100% AYUSH Verified Products"
      3. "Use code KAAVA10 for 10% off your first order"
    - Auto-rotate messages every 4 seconds using setInterval in useEffect (cleanup on unmount)
    - Use useState for currentIndex, cycle through messages array
    - Add CSS transition for opacity fade between messages (transition-opacity duration-500)
    - Respect prefers-reduced-motion by skipping animation (just show first message)
    - Export as default: `export default function AnnouncementBar()`

    **HomepageHeader.tsx** -- Must be a client component ("use client") because it uses useCart() hook.

    Create a full-width header with these specs:
    - Outer wrapper: w-full bg-white border-b border-kaava-border (Tailwind class for #F0F0F0)
    - Inner container: max-w-content mx-auto px-4 py-3, flex items-center justify-between
    - LEFT: Logo as text -- Link wrapping an h1 with "Kaava" in font-outfit text-2xl font-bold text-forest. Below it, small tagline span "Verified Ayurveda" in text-xs text-kaava-muted font-inter
    - CENTER: Search bar -- div with flex items-center, an input[type="text"] with placeholder "Search for products, brands, concerns..." styled with border border-kaava-border rounded-full px-4 py-2 w-full max-w-md text-sm font-inter focus:outline-none focus:border-saffron. Add a search icon (SVG magnifying glass inline, 16x16, text-kaava-muted) inside the div to the right of input
    - RIGHT: Nav links row with flex items-center gap-6
      - Links: "Concerns", "Brands", "About" as Link components from next/link with href="/", styled text-sm font-inter text-kaava-dark hover:text-forest transition-colors
      - Cart icon: button with relative positioning, an SVG shopping bag icon (24x24, text-kaava-dark), and a badge span -- absolute -top-1 -right-2 bg-saffron text-white text-xs rounded-full w-5 h-5 flex items-center justify-center font-inter font-semibold. Badge shows totalItems from useCart(). Only render badge when totalItems > 0. Button calls openDrawer on click.
    - Import Link from "next/link"
    - Import { useCart } from "@/context/CartContext"
    - Export as default: `export default function HomepageHeader()`
  </action>
  <verify>
    <automated>cd /Users/robindhiman/Desktop/Clients/HerbalMarketPlace/kaava-mvp && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File components/layout/AnnouncementBar.tsx exists and starts with "use client"
    - AnnouncementBar.tsx contains "bg-forest" class
    - AnnouncementBar.tsx contains "Free shipping on orders above" message string
    - AnnouncementBar.tsx contains "setInterval" for rotation logic
    - AnnouncementBar.tsx contains "export default function AnnouncementBar"
    - File components/layout/HomepageHeader.tsx exists and starts with "use client"
    - HomepageHeader.tsx contains 'import Link from "next/link"'
    - HomepageHeader.tsx contains 'import { useCart }' or 'import {useCart}'
    - HomepageHeader.tsx contains "totalItems" for badge count
    - HomepageHeader.tsx contains "openDrawer" for cart button click
    - HomepageHeader.tsx contains "bg-saffron" for badge styling
    - HomepageHeader.tsx contains "font-outfit" for logo styling
    - HomepageHeader.tsx contains "max-w-content" for container width
    - HomepageHeader.tsx contains "export default function HomepageHeader"
    - npx tsc --noEmit passes with zero errors related to these files
  </acceptance_criteria>
  <done>AnnouncementBar renders a rotating promo bar with 3 messages. HomepageHeader renders logo, search bar, nav links, and cart icon with dynamic badge count. Both are typed TypeScript with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AnnouncementBar into root layout</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (current root layout -- must preserve existing structure)
    - components/layout/AnnouncementBar.tsx (just created -- confirm export name)
    - node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md (layout conventions)
  </read_first>
  <action>
    Update app/layout.tsx to add the AnnouncementBar at the top of every page:

    1. Add import: `import AnnouncementBar from "@/components/layout/AnnouncementBar";`
    2. Inside the body tag, render AnnouncementBar ABOVE the CartProvider children:
       ```
       <body>
         <AnnouncementBar />
         <CartProvider>
           {children}
         </CartProvider>
       </body>
       ```
    3. Keep ALL existing code intact: metadata export, Google Fonts links in head, CartProvider wrapping children.
    4. Do NOT change the html lang, head contents, or metadata.
    5. AnnouncementBar goes OUTSIDE CartProvider because it does not need cart context. It goes inside body as the very first element.
  </action>
  <verify>
    <automated>cd /Users/robindhiman/Desktop/Clients/HerbalMarketPlace/kaava-mvp && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contains 'import AnnouncementBar from "@/components/layout/AnnouncementBar"'
    - app/layout.tsx contains "<AnnouncementBar" before "<CartProvider"
    - app/layout.tsx still contains 'import { CartProvider } from "@/context/CartContext"'
    - app/layout.tsx still contains the metadata export with "Kaava" in the title
    - app/layout.tsx still contains Google Fonts link tags in head
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>Root layout renders AnnouncementBar at the top of every page, above page content. All existing layout functionality preserved.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero TypeScript errors
2. `npm run build` does not fail on the layout components (may have other unrelated issues)
3. AnnouncementBar.tsx, HomepageHeader.tsx exist in components/layout/
4. app/layout.tsx imports and renders AnnouncementBar
</verification>

<success_criteria>
- AnnouncementBar component created with rotating promo messages, bg-forest styling
- HomepageHeader component created with logo, search bar, nav links, cart badge
- Root layout updated to show AnnouncementBar on every page
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-shell/01-01-SUMMARY.md`
</output>
